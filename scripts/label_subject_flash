#!/bin/tcsh -ef

setenv LTA   talairach.lta
setenv ASEG  aseg
setenv GCA $MRI_DIR/average/n7.gca

setenv s $1

set sdir = $SUBJECTS_DIR/$s/mri
mkdir -p $sdir/$ASEG
mri_ca_label  -flash $CSURF_DIR/average/tissue_parms.txt  $sdir/norm $sdir/transforms/$LTA $GCA $sdir/$ASEG
