/* Andrew Zhang
 * Professor Siless
 * dmri_groupingByEndpoints.cxx
 *
 */

#include <iostream>

#include "GetPot.h"

using namespace std;

int main(int narg, char* arg[]) 
{
/*	GetPot c1(narg, const_cast<char**>(arg));
	GetPot c12(narg, const_cast<char**>(arg)); 

	if (c1.size() == 1 || c1.search(2, "--help", "-h"))
	{
		cout << "Usage: " << endl; 
		cout << arg[0] << " -i anatomicutsFolder -n numClusters -c correspondenceFile -m <numMeasures> -o output" << endl; 
		return -1; 
	}*/
}
